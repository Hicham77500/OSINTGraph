name: Docker Build

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  validate-compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate docker-compose.yml
        run: docker compose config --quiet

      - name: Build images
        run: docker compose build

      - name: Backend compose smoke test
        run: |
          python -m pip install --quiet pytest
          cd backend && python -m pytest tests/test_docker_compose.py -q

# Optional GHCR push (requires GHCR_TOKEN secret and uncomment):
#  push-images:
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#    needs: validate-compose
#    runs-on: ubuntu-latest
#    permissions:
#      contents: read
#      packages: write
#    steps:
#      - uses: actions/checkout@v4
#      - uses: docker/login-action@v3
#        with:
#          registry: ghcr.io
#          username: ${{ github.actor }}
#          password: ${{ secrets.GITHUB_TOKEN }}
#      - run: |
#          docker compose build
#          docker tag osintgraph-api ghcr.io/${{ github.repository }}/api:latest
#          docker tag osintgraph-web ghcr.io/${{ github.repository }}/web:latest
#          docker push ghcr.io/${{ github.repository }}/api:latest
#          docker push ghcr.io/${{ github.repository }}/web:latest
# NAS: docker compose pull && docker compose up -d
